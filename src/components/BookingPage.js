import React from "react"
import BookingForm from "./BookingForm"

const BookingPage = ({ times }) => {
  return (
    <>
      BookingPage
      <BookingForm times={times} />
    </>
  )
}

export default BookingPage
